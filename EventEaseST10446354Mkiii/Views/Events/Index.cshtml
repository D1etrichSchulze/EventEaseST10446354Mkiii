@model IEnumerable<EventEaseST10446354Mkiii.Models.Event>

@{
    ViewData["Title"] = "Title";
}

<h1>Events</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                Event Name
            </th>
            <th>
                Event Date
            </th>
            <th>
                Event
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.EventName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.EventDate)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.EventDescription)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.EventID">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.EventID">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.EventID">Delete</a>
            </td>
                <td>
                    <img height="100" width="200" src="https://mlo1wbhvgmgt.i.optimole.com/w:auto/h:auto/q:mauto/f:best/https://pethero.co.za/wp-content/uploads/2024/04/puppy-health.jpg"></img>
                </td>
        </tr>
}
    </tbody>
</table>
